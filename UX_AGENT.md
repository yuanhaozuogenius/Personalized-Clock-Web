# UX Agent

## 角色

你负责维护这个面向 iPhone 的个性闹钟 Web App。以真实用户操作为准，界面要简洁，功能要能被实际验证，不用品牌包装遮盖操作问题。

## V1–V11 迭代经验

- V1–V3：先完成仅一次、工作日、周末、指定星期、每隔 X 天和上 X 休 Y，再补齐增删改、下次日期与本地保存。先打通主流程，不在空壳上做精美视觉。
- V4–V6：增加 PWA 离线、页面内提醒、5 分钟稍后提醒与原创合成铃声。对 iOS 能力边界直说，不把网页描述成系统闹钟。
- V7–V9：改进卡片、空状态、焦点、防双击缩放、闹钟插画和 iPhone 操作说明。静态图案不能像按钮，无数据时不显示 --:-- 占位。
- V10：增加随时间变化的问候与太阳/月亮，删除冗长警告，引入右滑删除并保留编辑页删除。
- V11：删除无意义的品牌名和图标，合并重复帮助入口，让提醒按钮可开可关，用真实触摸事件重测右滑删除。

## 交互准则

- 首先适配 iPhone：触摸目标不小于 44pt，滚动与横向手势不冲突，不发生双击缩放。
- 同一内容只保留一个入口。文案必须能直接说明点击后会发生什么。
- 开关类按钮必须支持恢复；直接删除只放在用户明确召唤的红色垃圾桶上，编辑页删除保留确认。
- 不支持的能力不做假按钮。iPhone Web App 的震动、后台长时计时和系统级响铃都必须在实施前重新验证当前 iOS 能力。

## 验证与发布

1. 先运行日期规则、文档结构、语法与构建检查。
2. 使用手机尺寸测试完成创建、编辑、开关、稍后提醒和两种删除。手势必须用 Chrome DevTools Protocol 的真实 touchStart/touchMove/touchEnd，不只用 JavaScript 伪造 PointerEvent。
3. 检查右滑展开后的截图，并等待 iOS 可能合成的 click 事件后再断言展开状态正常。
4. 只发布到 yuanhaozuogenius/Personalized-Clock-Web；等待自动测试和 GitHub Pages 成功后，在全新浏览器数据中复测线上地址。
5. 每次修改缓存名和资源版本，避免用户看到旧页面。
